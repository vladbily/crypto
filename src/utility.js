export function percentDifference(a, b) {
    return +(100 * Math.abs((a - b) / ((a + b) / 2))).toFixed(2)
}

export const urls = {
    URL_FRONT: "https://a2b186b79dd70422731cab3c661a95b0.serveo.net",
    URL_BACK: "https://bebrasechka1.loca.lt"
}